# Sing My Domain - Deployment Guide

Prerequisite: Python 3.12 or above

1. Run `pip install -r requirements.txt`

2. Copy `.env.example` to `.env`.

3. In `.env` replace the placeholder values with your real values.

4. Run `python app.py`

5. Access the app at: http://localhost:7860
