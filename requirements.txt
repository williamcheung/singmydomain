anyio==4.14.2
gradio==6.26.0
httpx==0.28.1
openai==3.6.0
# Pinned despite being transitive: gradio declares pydantic>=2.0,<=3.0 (very wide),
# which doesn't guarantee runtime compatibility with every release in that range.
pydantic==2.13.4
python-dotenv==1.2.3
tenacity==9.1.4

pytest==9.1.1
